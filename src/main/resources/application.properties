spring.application.name=ElectronicStore_29June

#
#spring.datasource.url=jdbc:mysql://localhost:3306/electronicstorejuneaddedsecurity
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.password=12345@shubham
#spring.datasource.username=root
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true

spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Port (Render will override with PORT env variable)
server.port=${PORT:8080}






#PathConfig

user.profile.image.path=images/users/
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

product.image.path=images/products/

